<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/padding_xlarge">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/wc_waiting_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/waitingH"
            android:textAlignment="center"
            android:textSize="@dimen/font_xlarge"
            android:textColor="@color/main_black"
            android:textStyle="bold"
            />

        <ProgressBar
            android:id="@+id/wc_waiting_loader"
            android:layout_width="@dimen/loader"
            android:layout_height="@dimen/loader"
            android:progressTint="@color/main_white"
            android:indeterminateTint="@color/main_black" />
    </LinearLayout>

</RelativeLayout>